import { inject, injectable } from 'inversify';
import * as ts from 'typescript';


@injectable()
export class SourceFileFactory implements ISourceFileFactory {
  constructor(
    @inject(EditorServiceName) private editorService: IEditorService
  ) { }

}





import { NodeCollection } from '../../models';

export const NodeFactoryName = 'INodeFactory';

export interface INodeFactory {
  createFromActiveWindow(): NodeCollection;
}
